const retorno = "Os números são iguais, tente outro número."

var soma = (a, b) => {return a + b}

const conferencia = (num1, num2) => {return num1 == num2 ? retorno : `${adicao}`}
adicao = soma(3,5);
console.log(conferencia(6, 7))

