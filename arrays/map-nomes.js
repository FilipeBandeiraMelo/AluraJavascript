let nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesAtualizados = nomes.map(name => name.toUpperCase());

console.log(nomesAtualizados);


const nome = "alura";

let nomeMaiusculas = nome[0].toUpperCase() + nome.slice(1);

console.log(nomeMaiusculas); // Alura